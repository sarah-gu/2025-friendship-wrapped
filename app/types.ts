import type { SubmissionModel } from "@/app/generated/prisma/models/Submission";

/**
 * Memory type that matches the Prisma Submission model
 * This is an alias for SubmissionModel to use "Memory" terminology in the app
 */
export type Memory = SubmissionModel;
